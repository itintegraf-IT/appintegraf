# Modul Projekty — stránky

Pravidla modulu: viz `components/projekty/CLAUDE.md`. Pro stránky zejména: RSC pro čtení dat, mutace přes `app/api/projekty/**`, UI výhradně z `components/projekty/ui/`, standardní design tokeny (nikdy `--notion-*`).
